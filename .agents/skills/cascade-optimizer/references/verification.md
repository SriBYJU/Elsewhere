# Verification

Machine evidence outranks model confidence. Progressive hierarchy: parser/compiler, type checker, targeted tests, package tests, integration tests, diff/scope checks, security checks, reviewer reasoning, head judgment. Never call a benchmark task done without its required deterministic acceptance criteria. Prefer minimal diffs.
