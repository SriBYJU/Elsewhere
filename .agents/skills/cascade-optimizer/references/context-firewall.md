# Context Firewall

Workers receive the minimum trustworthy evidence needed for a bounded task. Broad exploration happens outside the solver trajectory. Evidence must retain file/line provenance and trust labels. Exact lexical search, symbols/imports, dependency edges, git recency, and test relationships come before optional semantic retrieval. Raw transcripts are not forwarded automatically.
